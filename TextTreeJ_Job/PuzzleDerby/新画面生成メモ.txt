なんだかんだでやっぱ相当めんどい、雛形となるスクリプトを固定で使いまわした方がいい

とりあえず新画面に必要最低限なこととしては、MenuBaseを継承して
void Awake()
{
	curMenu  = this;
}

public override void In ()
{
	curMenu = this;
	CommonScrollBar.Instance.SetData( scrollList );
	base.In ();
｝

public override void Out ()
{
	FooterManager.Instance.HideSuggestPanel();
	
	if( curMenu == null ) return;
	
	if( curMenu == this )
	{
		MainMenuLogic.Instance.HideWebview();
		base.Out();
	}
	else
	{
		curMenu.Out();
	}
}

をいれること、これだけであとはまあなんとななる・・

とりあえず新画面ってのはプレハブありき、プレハブにMenuBaseを継承したスクリプトとUIScrollとBoxColliderをアタッチしなければならない
さらにMenuBaseのインスペクター、Objsに移動させたい子要素をアタッチすること。